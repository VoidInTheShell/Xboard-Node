name: us2-xboard-node

services:
  xboard-node:
    image: ${XBOARD_NODE_IMAGE:?XBOARD_NODE_IMAGE is required}
    container_name: xboard-node
    restart: unless-stopped
    init: true
    network_mode: host
    env_file:
      - ./runtime.env
    volumes:
      - ./config.yml:/etc/xboard-node/config.yml:ro
    mem_limit: 256m
    mem_reservation: 96m
    cpus: 1.0
    pids_limit: 256
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 20s
    logging:
      driver: json-file
      options:
        max-size: 5m
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:65530/healthz || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 20s

  xboard-node-edge:
    image: ${XBOARD_NODE_IMAGE:?XBOARD_NODE_IMAGE is required}
    container_name: xboard-node-edge
    restart: unless-stopped
    init: true
    network_mode: host
    entrypoint: ["/usr/bin/caddy"]
    command: ["run", "--config", "/etc/caddy/Caddyfile", "--adapter", "caddyfile"]
    env_file:
      - ./edge.env
    environment:
      XDG_CONFIG_HOME: /config
      XDG_DATA_HOME: /data
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./cover-public:/srv/cover:ro
      - ./caddy-data:/data
      - ./caddy-config:/config
    mem_limit: 96m
    mem_reservation: 24m
    cpus: 0.5
    pids_limit: 96
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 15s
    logging:
      driver: json-file
      options:
        max-size: 5m
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent --show-error --resolve \"$${NODE_DOMAIN}:443:127.0.0.1\" \"https://$${NODE_DOMAIN}/\" > /dev/null || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 12
      start_period: 30s
